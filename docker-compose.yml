version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=production
      - PORT=5173
      - HOST=0.0.0.0
      - DATABASE_URL=postgres://postgres:partysecret@db:5432/partyvote
      - PUBLIC_ORIGIN=${PUBLIC_ORIGIN:-} # Optional: e.g. http://192.168.1.100:5173 or https://party.yourdomain.com
    depends_on:
      db:
        condition: service_healthy
    restart: always

  db:
    image: postgres:16-alpine
    container_name: partyvote_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: partysecret
      POSTGRES_DB: partyvote
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

volumes:
  pgdata:
